# Roadmap

## CURRENT STEP (resume here)

- Backend running with:
  - `/api/dashboard` using services/portfolio.py
  - `/api/holdings` using FAKE_HOLDINGS
- Next: add SQLite DB (db.py, models.py) and move holdings into `user_data/robo_advisor.db`.
